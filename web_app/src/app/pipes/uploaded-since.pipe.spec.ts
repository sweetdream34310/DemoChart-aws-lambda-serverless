import { UploadedSincePipe } from './uploaded-since.pipe';

describe('UploadedSincePipe', () => {
  it('create an instance', () => {
    const pipe = new UploadedSincePipe();
    expect(pipe).toBeTruthy();
  });
});
