import { LimitNgForPipe } from './limit-ng-for.pipe';

describe('LimitNgForPipe', () => {
  it('create an instance', () => {
    const pipe = new LimitNgForPipe();
    expect(pipe).toBeTruthy();
  });
});
